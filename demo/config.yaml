queue:
  concurrency: 8
  max_attempts: 5
  timeout_ms: 15000
  backoff_ms: 500
  backoff_factor: 2

logging:
  level: debug
  destination: stdout
  include_task_id: true

metrics:
  enabled: true
  interval_ms: 30000
  exporter: prometheus
